class <%= name.camelize %>Mapping < Grape::JsonApi::Mapping
  <% @properties.each do |prop| %>
    property <%= ":#{prop}" -%>
  <% end -%>
  <% @relationships.each do |rel_type, rels| %>
  <% rels.each do |prop| %>
    <%= "#{rel_type} :#{prop.name}" -%>
  <% end -%>
  <% end %>

end
